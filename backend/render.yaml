services:
  - type: web
    name: grocery-store-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python simple_app.py
    envVars:
      - key: FLASK_ENV
        value: production
